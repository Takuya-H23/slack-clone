import { createMuiTheme } from '@material-ui/core/styles'

export const lightTheme = createMuiTheme({
  palette: {
    text: {
      primary: '#1d1d1d',
      secondary: '#ffffff'
    },
    background: {
      default: '#4a154b',
      paper: '#f6efe8'
    }
  }
})
